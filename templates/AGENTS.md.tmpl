# {{PROJECT_NAME}} — 项目规范

> **本文件是所有 AI Agent（Claude Code / Codex CLI / Gemini CLI）的项目级规范。**
> `./CLAUDE.md` 是本文件的符号链接，确保 Claude Code 读到相同内容。
> `./CLAUDE.local.md` 存放个人本地配置（不提交 git）。
> 通用的多 Agent 工作流规范见 `.vk/workflow.md`。

**请先阅读 [.vk/workflow.md](.vk/workflow.md) 了解多 Agent 并行开发工作流规范。**

---

## 1. 项目概述

**{{PROJECT_NAME}}** — {{PROJECT_DESCRIPTION}}

- 当前版本：{{PROJECT_VERSION}}

---

## 2. 技术栈

| 层 | 技术 |
|---|------|
| 后端 | {{BACKEND_STACK}} |
| 前端 | {{FRONTEND_STACK}} |
| 数据库 | {{DATABASE_STACK}} |
| 部署 | {{DEPLOY_STACK}} |
| 包管理 | {{PACKAGE_MANAGERS}} |

---

## 3. 目录结构与模块边界

```
{{PROJECT_NAME}}/
{{DIRECTORY_STRUCTURE}}
```

### 模块所有权规则

- 公共/共享模块修改需最高优先级完成，其他模块依赖它
- 每个独立模块是一个 VK Workspace 单元
- 数据库 schema 修改必须附带迁移说明

---

## 4. 编码规范

{{CODING_CONVENTIONS}}

---

## 5. 构建/测试命令

```bash
{{BUILD_COMMANDS}}
```

### VK 仓库脚本配置

- **Setup Script**: `{{VK_SETUP_SCRIPT}}`
- **Cleanup Script**: `bash scripts/agent-quality-gate.sh`
- **Dev Server**: `{{VK_DEV_SERVER}}`

---

## 6. 工作前必读（避坑指南）

> **规则：在编写或审查对应类型的代码前，必须先读取相关避坑指南。**
> **规则：在排查问题前，必须优先读取相关避坑指南。**

经验库统一存放在 `multi-agent-workflow/agent-global/pitfalls/`（通过 `make sync-knowledge` 跨设备同步）。

| 触发场景 | 指南文件 |
|---------|----------|
| **任何涉及进程/状态/外部服务的问题排查** | `agent-global/pitfalls/debugging.md` |
| **网络/服务访问异常（502/503/超时/SSL）** | `agent-global/pitfalls/network-service-check.md` |
| 编写/审查 Shell 脚本、Makefile | `agent-global/pitfalls/shell.md` |
| 编写/审查 Python 代码（ORM、异常、数据类）| `agent-global/pitfalls/python.md` |
| 编写/审查前端代码（pnpm、TypeScript、Vue）| `agent-global/pitfalls/frontend.md` |
| 调试 dispatcher / 状态机 / 幂等资源管理 | `agent-global/pitfalls/dispatcher.md` |
| K8s/K3s 镜像、Helm、网络 | `agent-global/pitfalls/k8s.md` |

> 新发现的坑追加到对应文件末尾，编号全局递增（PIT-XXX）。
> Claude Code 用户：`~/.claude/pitfalls/` 已通过符号链接指向上述目录，会自动加载。

---

## 7. 禁止操作清单

| 禁止操作 | 原因 |
|---------|------|
{{FORBIDDEN_OPERATIONS}}
