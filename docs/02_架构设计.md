# 架构设计文档

> 项目：multi-agent-workflow
> 版本：v1.0
> 日期：2026-06-19
> 状态：已确认（v1.1 — SQLite 混合检测）

---

## 1. 架构总览

### 1.1 设计哲学

- **最小侵入**：通过模板注入，无需修改目标项目的业务逻辑
- **零依赖**：调度器纯 Python 标准库实现，无需安装额外包
- **状态机驱动**：所有自动化以 VK Issue 状态变迁为唯一触发源
- **补偿优先**：每轮轮询检查未完成动作，崩溃恢复后自动续跑
- **可观测性内建**：每轮附 trace_id，指标累积，日志结构化

### 1.2 整体架构图

```
┌──────────────────────────────────────────────────────────────┐
│                   用户输入层                                   │
│                                                              │
│   Human        VS Code Copilot (Plan Mode)                   │
│   ─────        ──────────────────────────                    │
│   创建需求      分析代码库 → 架构图 → 任务拆解                  │
│                通过 VK MCP 创建 Issue                         │
└──────────────────────────┬───────────────────────────────────┘
                           │ MCP: create_task / start_workspace
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                   Vibe Kanban (外部服务)                       │
│                                                              │
│   ┌──────────┐  ┌────────────┐  ┌───────────────────────┐   │
│   │ 看板状态  │  │ Workspace  │  │   Git Worktree 管理    │   │
│   │ To do    │  │ Session    │  │   分支创建/切换         │   │
│   │ In prog  │  │ Agent 绑定  │  │   并行隔离             │   │
│   │ In review│  └────────────┘  └───────────────────────┘   │
│   │ Done     │                                               │
│   └──────────┘                                               │
│                                                              │
│   REST API (:9527)  ←────────────── MCP stdio               │
└──────────────────────┬───────────────────────────────────────┘
                       │ 轮询 REST API
                       ▼
┌──────────────────────────────────────────────────────────────┐
│              Central Dispatcher（核心层）                     │
│                                                              │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                    状态机引擎                         │   │
│   │                                                     │   │
│   │  To do ──[auto_start_coding]──► 启动编码 Session     │   │
│   │                                                     │   │
│   │  In review ──[auto_create_pr]──► 创建 GitHub PR      │   │
│   │           ──[auto_start_review]─► 启动审查 Session   │   │
│   │                                                     │   │
│   │  Done ──[auto_merge]──────────► merge PR            │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                              │
│   ┌────────────────┐    ┌───────────────┐                   │
│   │  补偿检查器     │    │  状态持久化    │                   │
│   │  每轮扫描遗漏   │    │  dispatcher_  │                   │
│   │  动作自动重试   │    │  state.json   │                   │
│   └────────────────┘    └───────────────┘                   │
│                                                              │
│         ┌───────────────────┬───────────────────┐           │
│         │                   │                   │           │
│         ▼                   ▼                   ▼           │
│   ┌──────────┐       ┌──────────┐       ┌──────────┐       │
│   │  vk.py   │       │github.py │       │ 日志/指标 │       │
│   │ REST 客户 │       │ PR 生命  │       │ trace_id │       │
│   │ MCP stdio │       │ 周期管理  │       │ 累积指标  │       │
│   └──────────┘       └──────────┘       └──────────┘       │
└───────────────────────────┬──────────────────────────────────┘
                            │ subprocess / Workspace API
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                     Agent 执行层                              │
│                                                              │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│   │ Claude Code │  │  Codex CLI  │  │  Gemini CLI │        │
│   │             │  │             │  │             │        │
│   │  编码/审查   │  │  编码/审查   │  │  编码/审查   │        │
│   │  worktree-1 │  │  worktree-2 │  │  worktree-3 │        │
│   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘        │
│          │                │                │               │
│          └────────────────┴────────────────┘               │
│                           │                                 │
│                     vk-hooks.sh                             │
│               (cleanup 钩子，质量门禁)                       │
│                           │                                 │
│         lint + test 通过 → push 分支 → 状态 → In review      │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                     GitHub（外部服务）                         │
│                                                              │
│   Pull Request  ←─── Dispatcher 创建                         │
│   Code Review   ←─── 审查 Agent 提交评论                      │
│   Merge PR      ←─── Dispatcher squash merge                │
└──────────────────────────────────────────────────────────────┘
```

---

## 2. 模块设计

### 2.1 模块划分

```
multi-agent-workflow/
├── init.sh                         # [初始化层] 一键注入工作流到目标项目
├── dispatcher/                     # [调度层] 中央调度器 Python 包
│   ├── __init__.py
│   ├── __main__.py                 # CLI 入口：python -m dispatcher
│   ├── core.py                     # 状态机引擎 + 补偿逻辑
│   ├── vk.py                       # VK 双客户端（REST + MCP）
│   ├── vk_db.py                    # VK SQLite 直读客户端（进程状态检测）
│   └── github.py                   # GitHub REST API v3 客户端
├── templates/                      # [模板层] 注入到目标项目的文件模板
│   ├── CLAUDE.md.tmpl
│   ├── gitignore.append
│   ├── Makefile.append
│   └── vk/
│       ├── workflow.md             # 工作流规范（Agent 阅读文件）
│       ├── dispatcher.json         # 调度器配置模板
│       └── prompts/                # Agent 提示词
│           ├── coder.md
│           ├── reviewer.md
│           └── planner.md
└── scripts/                        # [质量保障层] 各类钩子脚本
    ├── vk-hooks.sh                 # 编码完成钩子（质量门禁入口）
    ├── agent-quality-gate.sh       # lint + 测试执行
    ├── check-worktree-conflicts.sh # 冲突检测
    └── post-merge-verify.sh        # 合并后回归验证
```

### 2.2 模块职责边界

| 模块 | 职责 | 不负责 |
|------|------|--------|
| `init.sh` | 文件注入、配置生成 | 任务执行、运行时调度 |
| `core.py` | 状态机、事件分发、补偿 | HTTP 实现细节 |
| `vk.py` | VK REST 轮询、MCP 会话创建 | 状态判断逻辑 |
| `vk_db.py` | 直读 VK SQLite，检测 Agent 运行/完成状态 | Issue 管理、PR 创建 |
| `github.py` | PR CRUD、merge | Issue 管理、看板 |
| `vk-hooks.sh` | 编码后质量门禁、分支 push | Agent 启动 |

---

## 3. 核心状态机

```
                  ┌───────────┐
                  │  To do    │
                  └─────┬─────┘
                        │ auto_start_coding = true
                        │ 创建编码 Session（VK MCP）
                        ▼
                  ┌───────────┐
                  │In progress│  ←── Agent 正在编码
                  └─────┬─────┘
                        │ vk-hooks.sh: lint+test 通过
                        │ push → cleanup_script 完成
                        │ Dispatcher 轮询 SQLite → QG passed
                        ▼
                  ┌───────────┐
   Dispatcher:    │ In review │
   ① 状态流转 ───►│           │◄── Dispatcher 检测到 QG 通过后触发
   ② create_pr    └─────┬─────┘
   ③ start_review
                        │ 审查 Agent 审查通过
                        │ VK 状态更新 → Done
                        ▼
                  ┌───────────┐
   Dispatcher:    │   Done    │
   merge PR ─────►│           │
                  └───────────┘
```

### 状态转换条件一览

| 源状态 | 目标状态 | 触发者 | 前置条件 | 调度器动作 |
|--------|----------|--------|----------|------------|
| To do | In progress | Dispatcher | `auto_start_coding=true` | 创建编码 Session |
| In progress | In review | Dispatcher | SQLite: QG 通过（cleanup=completed 或 codingagent=completed） | ① 状态→In review ② 创建 PR ③ 创建审查 Session |
| In review | Done | 审查 Agent | 审查通过 | merge PR |
| Done | — | — | PR 已合并 | 无 |

### QG 通过检测逻辑（REST + SQLite 混合）

Dispatcher 在每轮补偿检查中调用 `VKDatabase.is_qg_passed(branch)` 判断编码是否完成：

```
┌── repo 配置了 cleanup_script ？──────────────────────┐
│  YES                                NO               │
│  检测 cleanupscript 进程             检测 codingagent 进程 │
│  status='completed' AND exit_code=0  status='completed' AND exit_code=0 │
└──────────────────────────────────────────────────────┘
         ↓ True                              ↓ True
   Dispatcher._action_finish_coding() → In review
```

**关键 SQL 约束**（过滤 VK 重启假失败）：
- `ep.dropped = FALSE`（排除已丢弃进程）
- `exit_code IS NOT NULL`（`NULL` = VK 重启 artifact，非真实失败）
- `ORDER BY started_at DESC LIMIT 1`（取最新一条）

**三态返回**：`True`（通过）/ `False`（真实失败，等待人工）/ `None`（运行中或未开始）

### 补偿场景

| 检测条件 | 补偿动作 |
|----------|----------|
| 状态=In progress 且有 `coding_branch` 且 SQLite QG=True | 状态→In review，创建 PR，创建审查 Session |
| 状态=In progress 且有 `coding_branch` 且 SQLite QG=False | 记录警告，等待人工介入 |
| 状态=In review 且 `pr_number=null` 且有 `coding_branch` | 创建 PR |
| 状态=In review 且 `review_workspace_id=null` | 创建审查 Session |
| 状态=Done 且 `pr_merged=false` 且有 `pr_number` | 合并 PR |

---

## 4. 数据模型

### 4.1 IssueTracker（调度器内存 + 持久化）

```python
@dataclass
class IssueTracker:
    status: str                      # 当前 Issue 状态
    title: str                       # Issue 标题
    simple_id: str                   # 人类可读 ID（如 PROJ-42）
    # 编码阶段
    coding_workspace_id: str | None  # VK Workspace ID
    coding_branch: str | None        # 编码分支名
    coder_executor: str | None       # 编码 Agent 类型
    # PR
    pr_number: int | None            # GitHub PR 号
    pr_url: str | None               # PR 链接
    pr_merged: bool                  # 是否已合并
    # 审查阶段
    review_workspace_id: str | None  # 审查 Session Workspace ID
    review_branch: str | None        # 审查分支名
    # 合并
    merged: bool                     # 最终是否已合并
    # 时间戳
    updated_at: str                  # 最后更新时间

# 注: v1.0 的 in_progress_since / last_qg_sha 已在 v1.1 移除。
# QG 状态改由 VKDatabase.is_qg_passed() 直读 VK SQLite 获取，无需文件标记。
```

持久化路径：`{project_dir}/.vk/dispatcher_state.json`

### 4.2 DispatcherConfig（配置文件）

对应模板：`templates/vk/dispatcher.json`

```jsonc
{
  "organization_id": "...",          // VK 组织 ID
  "project_id": "...",               // VK 项目 ID
  "repo_id": "...",                  // VK 仓库 ID
  "main_branch": "master",           // 主分支名
  "poll_interval": 30,               // 轮询间隔（秒）
  "vk_port": 9527,                   // VK 服务端口

  // 自动化开关
  "auto_start_coding": false,        // To do → 自动启动编码（默认关闭）
  "auto_create_pr": true,            // In review → 自动创建 PR
  "auto_start_review": true,         // In review → 自动启动审查
  "auto_merge": true,                // Done → 自动合并 PR

  // PR 配置
  // 注: v1.0 的 qg_fallback_timeout_seconds 已在 v1.1 移除，由 SQLite 直读取代
  "pr_merge_method": "squash",       // squash / merge / rebase
  "pr_draft": false,

  // Agent 配置
  "default_coder_executor": "CLAUDE_CODE",
  "cross_review_map": {
    "CLAUDE_CODE": "CODEX",
    "CODEX": "CLAUDE_CODE",
    "GEMINI": "CODEX"
  }
}
```

---

## 5. 关键接口

### 5.1 VKRestClient

```python
class VKRestClient:
    def health_check() -> bool
    def list_issues(project_id: str, limit: int) -> list[dict]
    def update_issue_status(issue_id: str, status_id: str) -> dict
```

调用端点：`http://127.0.0.1:{vk_port}/api/remote/...`

### 5.2 VKDatabase（SQLite 直读）

```python
class VKDatabase:
    """直读 VK 本地 SQLite（~/.local/share/vibe-kanban/db.v2.sqlite）"""

    def is_qg_passed(self, branch: str) -> bool | None:
        """检测指定分支的 QG 是否通过。

        1. 若 repo 配置了 cleanup_script → 以 cleanupscript 进程完成为准
        2. 否则 → 以 codingagent 进程成功完成为准

        返回:
          True  — QG 通过（可流转 In review）
          False — Agent 真实失败（exit_code IS NOT NULL AND exit_code != 0）
          None  — 仍在运行 / 尚未启动 / 数据不足
        """

    def is_agent_running(self, branch: str) -> bool:
        """检查编码 Agent 是否仍在运行（dropped=FALSE AND status='running'）"""

    def has_cleanup_script(self, branch: str) -> bool:
        """检查关联仓库是否配置了 cleanup_script"""

    def get_latest_process(self, branch: str, run_reason: str) -> dict | None:
        """获取指定分支、指定 run_reason 的最新执行记录"""
```

**SQLite 路径**：`~/.local/share/vibe-kanban/db.v2.sqlite`

**重要约束**：
- 所有查询必须加 `ep.dropped = FALSE`
- `exit_code IS NULL` = VK 重启 artifact（`cleanup_orphan_executions`），不视为失败
- VK remote 项目模式下 `tasks` 表为空；Issue 状态只能通过 REST API 获取

### 5.3 VKMCPClient（MCP stdio）

```python
class VKMCPClient:
    def create_workspace_session(workspace_id: str, prompt: str, executor: str) -> dict
    def list_workspaces(organization_id: str) -> list[dict]
```

启动方式：`npx -y vibe-kanban@latest --mcp` 子进程，JSON-RPC over stdio

### 5.3 GitHubClient

```python
class GitHubClient:
    @classmethod
    def from_project(project_dir: str) -> GitHubClient   # 自动检测 owner/repo/token

    def create_pr(head, base, title, body, draft) -> dict
    def get_pr(pr_number) -> dict
    def merge_pr(pr_number, merge_method) -> dict
    def add_review_comment(pr_number, body) -> dict
    def list_pull_requests(state, head) -> list[dict]
```

认证：`GITHUB_TOKEN` 环境变量 或 `.vk/github_token` 文件

---

## 6. 部署模型

### 6.1 本地开发部署（典型场景）

```
开发机

 ┌─────────────────────────────────────────────────────┐
 │  VS Code                                            │
 │  ├── Copilot (Plan Mode) — MCP → VK                 │
 │  └── 终端                                           │
 │       ├── npx vibe-kanban         (端口 9527)        │
 │       ├── python -m dispatcher   (后台轮询进程)       │
 │       ├── claude / codex / gemini (Agent 进程)       │
 │       └── make quality-gate      (按需运行)           │
 └────────────────────────┬────────────────────────────┘
                          │ HTTPS
                          ▼
              GitHub.com (PR / Merge)
```

### 6.2 目标项目初始化后的文件层级

```
target-project/
├── CLAUDE.md               # 项目规范（Agent 阅读入口）
├── AGENTS.md               # 符号链接 → CLAUDE.md
├── CLAUDE.local.md         # 个人配置（不提交 git）
├── .vscode/
│   └── mcp.json            # VS Code MCP Server 配置
├── .vk/
│   ├── workflow.md         # 工作流规范（Agent 阅读）
│   ├── dispatcher.json     # 调度器配置
│   ├── dispatcher_state.json  # 运行时状态（不提交 git）
│   ├── status_map.json     # VK 状态 ID 映射
│   ├── github_token        # GitHub Token（不提交 git）
│   └── prompts/
│       ├── coder.md
│       ├── reviewer.md
│       └── planner.md
├── scripts/
│   ├── vk-hooks.sh
│   ├── agent-quality-gate.sh
│   ├── check-worktree-conflicts.sh
│   └── post-merge-verify.sh
├── .gitignore              # 已追加 .vk/dispatcher_state.json 等
└── Makefile                # 已追加 dispatcher / quality-gate 等目标
```

---

## 7. 安全设计

| 攻击面 | 防护措施 |
|--------|----------|
| GitHub Token 泄漏 | 写入 `.vk/github_token`，在 `.gitignore` 中排除 |
| VK 端口越权 | VK 绑定 127.0.0.1，仅本地访问 |
| 恶意 Agent 代码提交 | 质量门禁 `lint + test` 阻断，不通过不 push |
| PR 误合并 | `auto_merge` 默认开启但状态机严格校验 Issue 状态 |
| 重复执行 | `pr_number` / `pr_merged` 字段幂等保护 |

---

## 8. 可观测性设计

### 8.1 日志结构

```
[2026-03-03 10:23:45] [INFO ] [a3f1c2] 轮询 #12: 5 issues, 1 变化 (累计: 8 动作, 0 错误)
[2026-03-03 10:23:45] [INFO ] [a3f1c2] 状态变化: PROJ-3「修复登录 bug」In progress → In review
[2026-03-03 10:23:46] [INFO ] [a3f1c2] ▸ PROJ-3: 创建 PR #17 → https://github.com/.../pull/17
[2026-03-03 10:23:46] [INFO ] [a3f1c2] ▸ PROJ-3: 自动创建审查 Session
```

字段：时间戳 | 日志级别 | trace_id（6位）| issue simple_id | 操作摘要

### 8.2 运行指标

| 指标 | 说明 |
|------|------|
| `poll_count` | 累计轮询次数 |
| `action_count` | 累计成功动作数 |
| `error_count` | 累计错误次数 |

### 8.3 状态持久化

`dispatcher_state.json` 存储 `issue_id → IssueTracker` 映射，用于：
- 崩溃恢复后补偿未完成动作
- 去重已执行动作，避免重复触发

---

## 9. 扩展设计

### 9.1 新增 Agent 类型

在 `dispatcher.json` 的 `cross_review_map` 中增加条目：

```json
"cross_review_map": {
  "MY_NEW_AGENT": "CLAUDE_CODE"
}
```

无需改动代码。

### 9.2 自定义质量门禁

编辑目标项目的 `scripts/agent-quality-gate.sh`，替换 lint/test 命令：

```bash
# 替换为项目实际命令
uv run pytest tests/
uv run ruff check .
pnpm test
```

### 9.3 多项目并行调度

每个目标项目运行独立的 Dispatcher 进程，互不干扰：

```bash
# 项目 A
cd ~/Workflow/project-a && python -m dispatcher

# 项目 B
cd ~/Workflow/project-b && python -m dispatcher
```

---

## 10. 技术选型决策记录（ADR）

### ADR-001：纯标准库实现调度器
- **决策**：Dispatcher 仅使用 Python 标准库（`urllib`、`subprocess`、`json`）
- **原因**：零 pip 依赖，在任意 Python 3.10+ 环境即可运行；降低注入到目标项目的侵入性
- **代价**：HTTP 客户端稍冗长，无 retry 中间件，需自行实现补偿

### ADR-002：VK 作为唯一任务编排中枢
- **决策**：所有任务状态变更通过 VK 看板，Dispatcher 被动监听
- **原因**：避免状态分裂；允许人工在看板上干预任何阶段；VK 提供 MCP 接口供 AI 直接操作
- **代价**：Dispatcher 完全依赖 VK 可用性，VK 宕机则自动化停止

### ADR-003：状态机 + 补偿优于事件 Webhook
- **决策**：使用轮询 + 幂等补偿，而非 VK Webhook 推送
- **原因**：VK 当前版本不提供 Webhook；轮询 + 持久化状态机对进程崩溃更鲁棒
- **代价**：状态变化响应有最多 `poll_interval` 秒延迟（默认 30s）

### ADR-004：GitHub squash merge 为默认合并方式
- **决策**：PR 默认使用 squash merge
- **原因**：保持主分支提交历史整洁；Agent 并行开发产生的中间提交不进入主分支
- **代价**：feature 分支的细粒度提交历史在主分支不可见
### ADR-005：用 SQLite 直读替代文件标记检测 QG 状态（v1.1）
- **决策**：删除 `.vk/qg_passed/<sha>` 文件标记系统和 QG 兜底执行器；改为直读 VK 本地 SQLite DB
- **背景**：标记文件在 Dispatcher 重启后持久化 → 造成 `In progress → In review` ping-pong；QG 兜底执行器在 worktree 中运行业务脚本破坏了"Dispatcher 不执行业务逻辑"的职责边界
- **方案对比**：
  - REST API only：`/api/remote/workspaces/{id}` 只返回快照，无进程历史，running 状态不可靠
  - SQLite only：`execution_processes` 表完整记录所有进程状态和 exit_code，是权威来源
  - **REST + SQLite hybrid（选用）**：Issue 状态用 REST（remote 模式 `tasks` 表为空），Agent 运行/完成状态用 SQLite
- **为何 SQLite 可靠**：VK cleanup_script 仅在 codingagent 正常完成（exit_code=0）后触发；`exit_code=NULL` = VK 重启 artifact（`cleanup_orphan_executions()`），非真实失败；结论来自 VK 源码分析（`crates/container.rs`、`crates/execution_process.rs`）
- **代价**：依赖 VK SQLite DB 路径约定（`~/.local/share/vibe-kanban/db.v2.sqlite`）；DB 路径随 VK 版本变化需同步更新